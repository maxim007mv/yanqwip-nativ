# Yanqwip Merged Backend

Объединённый бэкенд с полной функциональностью из обеих версий проекта.

## ✨ Возможности

### Из `old_backend`:
- ✅ **Полноценная авторизация** - JWT токены, регистрация, refresh tokens
- ✅ **AI-генерация маршрутов** - интеграция с DeepSeek API
- ✅ **Интеграция с Yandex Maps API** - геокодинг и карты
- ✅ **AI-чат агент** - интеллектуальный помощник
- ✅ **Сохранение маршрутов** - персональная история пользователя

### Из `backend1`:
- ✅ **Работа с PostgreSQL** - реальные места и заведения
- ✅ **База данных мест** - places, cuisines, amenities, price_categories
- ✅ **Фильтры и поиск** - по типу, кухне, цене, рейтингу
- ✅ **Геопоиск** - поиск мест по координатам

## 📁 Структура проекта

```
merged_backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # Главный файл приложения
│   ├── config.py            # Конфигурация (PostgreSQL + API ключи)
│   ├── database.py          # Подключение к PostgreSQL
│   ├── models.py            # SQLAlchemy модели (users + places)
│   ├── schemas.py           # Pydantic схемы
│   ├── security.py          # JWT авторизация
│   ├── deps.py              # Dependency injection
│   ├── routers/
│   │   ├── auth.py          # Регистрация/вход
│   │   ├── routes.py        # Генерация и управление маршрутами
│   │   ├── places.py        # Работа с местами
│   │   └── agent.py         # AI-чат агент
│   └── services/
│       ├── deepseek.py      # DeepSeek AI интеграция
│       ├── yandex.py        # Yandex Maps API
│       └── overpass.py      # OpenStreetMap данные
├── init_db.py               # Скрипт инициализации таблиц пользователей
├── requirements.txt         # Зависимости
├── .env                     # Конфигурация (создайте из .env.example)
└── README.md

```

## 🚀 Установка и запуск

### 1. Установите зависимости

```powershell
cd merged_backend
pip install -r requirements.txt
```

### 2. Настройте .env файл

Скопируйте `.env.example` в `.env` и заполните:

```env
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/yanqwip
SECRET_KEY=your-secret-key
DEEPSEEK_API_KEY=your-deepseek-api-key
YANDEX_API_KEY=your-yandex-api-key
```

### 3. Инициализируйте таблицы пользователей

**ВАЖНО:** Таблицы мест (places, cuisines и т.д.) уже существуют в вашей БД!
Этот скрипт создаёт только таблицы для пользователей:

```powershell
python init_db.py
```

Будут созданы таблицы:
- `users` - пользователи
- `routes` - маршруты пользователей
- `refresh_tokens` - токены обновления
- `agent_messages` - история чата с AI
- `generation_jobs` - задачи генерации

### 4. Запустите сервер

```powershell
python -m app.main
```

Или через uvicorn:

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен на:
- **API**: http://localhost:8000
- **Документация**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

## 📚 API Endpoints

### Авторизация (`/api/auth`)
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход (получить токены)
- `POST /api/auth/refresh` - Обновить access token
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь
- `PUT /api/auth/me` - Обновить профиль

### Маршруты (`/api/routes`)
- `POST /api/routes/generate` - Генерировать маршрут (AI)
- `POST /api/routes/save` - Сохранить маршрут
- `GET /api/routes/user` - Мои маршруты
- `GET /api/routes/{route_id}` - Получить маршрут
- `DELETE /api/routes/{route_id}` - Удалить маршрут

### Места (`/api/places`)
- `GET /api/places/search` - Поиск мест с фильтрами
- `GET /api/places/{place_id}` - Детали места
- `GET /api/places/types/list` - Типы мест
- `GET /api/places/cuisines/list` - Типы кухонь
- `GET /api/places/amenities/list` - Удобства

### AI-агент (`/api/agent`)
- `POST /api/agent/message` - Отправить сообщение агенту
- `GET /api/agent/history` - История чата

## 🔑 Получение API ключей

### DeepSeek API
1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Создайте API ключ
3. Добавьте в `.env`: `DEEPSEEK_API_KEY=sk-...`

### Yandex Maps API
1. Зарегистрируйтесь на https://developer.tech.yandex.ru/
2. Создайте приложение и получите ключ
3. Добавьте в `.env`: `YANDEX_API_KEY=...`

## 🗄️ База данных

### Существующие таблицы (из backend1):
- `places` - места и заведения
- `place_types` - типы мест
- `cuisines` - типы кухонь
- `amenities` - удобства
- `price_categories` - ценовые категории
- И другие связанные таблицы

### Новые таблицы (для пользователей):
- `users` - пользователи
- `routes` - маршруты
- `refresh_tokens` - токены
- `agent_messages` - чат с AI
- `generation_jobs` - задачи генерации

## 🧪 Тестирование

### Быстрый тест
```powershell
# Health check
curl http://localhost:8000/health

# Регистрация
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123","full_name":"Test User"}'

# Вход
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'
```

### Интерактивная документация
Откройте http://localhost:8000/docs для тестирования всех endpoints.

## 🔧 Troubleshooting

### Ошибка подключения к БД
```
Проверьте:
1. Запущен ли PostgreSQL: Get-Service postgresql*
2. Правильный ли пароль в DATABASE_URL
3. Существует ли база данных 'yanqwip'
```

### Ошибка импорта модулей
```powershell
# Установите зависимости заново
pip install -r requirements.txt --upgrade
```

### Ошибка "Table already exists"
```
Это нормально! Таблицы мест уже существуют.
init_db.py создаст только таблицы пользователей.
```

## 📝 Различия от предыдущих версий

### vs `old_backend`
- ✅ Использует PostgreSQL вместо SQLite
- ✅ Работает с реальными местами из БД
- ✅ Упрощённая архитектура (без jobs/async генерации)

### vs `backend1`
- ✅ Добавлена полная авторизация
- ✅ Добавлена AI-генерация маршрутов
- ✅ Добавлен AI-чат агент
- ✅ Использует SQLAlchemy ORM

## 🎯 Следующие шаги

1. ✅ Запустите backend: `python -m app.main`
2. ✅ Проверьте API docs: http://localhost:8000/docs
3. ✅ Зарегистрируйте тестового пользователя
4. ✅ Сгенерируйте маршрут через AI
5. ✅ Подключите frontend приложение

## 🚀 Деплой на Heroku

Бэкенд готов к деплою на Heroku! Все необходимые файлы созданы:
- ✅ `Procfile` - команда запуска для Heroku
- ✅ `runtime.txt` - версия Python
- ✅ `requirements.txt` - зависимости
- ✅ `.gitignore` - игнорируемые файлы
- ✅ Поддержка Heroku PostgreSQL и Config Vars

### 📖 Инструкции по деплою:
- **Подробная инструкция**: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Быстрая шпаргалка**: [HEROKU_QUICKSTART.md](./HEROKU_QUICKSTART.md)

### ⚡ Быстрый деплой (команды):
```bash
cd merged_backend
heroku login
heroku create yanqwip-backend
heroku addons:create heroku-postgresql:essential-0
heroku config:set SECRET_KEY="ваш-секретный-ключ"
heroku config:set DEEPSEEK_API_KEY="sk-..."
heroku config:set YANDEX_API_KEY="..."
git add .
git commit -m "Deploy to Heroku"
git push heroku main
heroku run python init_db.py
heroku open
```

После деплоя обновите `frontend/.env`:
```
API_URL=https://yanqwip-backend.herokuapp.com
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Проверьте .env конфигурацию
3. Проверьте подключение к PostgreSQL
4. Проверьте API ключи

---

**Версия**: 2.0.0  
**Дата создания**: Ноябрь 2025  
**Статус**: ✅ Готов к использованию
